@model IEnumerable<ShopBanHang.Models.CTHDOff>


<table class="table">
    <tr>
        <th>
            Số lượng
        </th>
        <th>
            Thành tiền
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.thanhTien)
        </td>
        
    </tr>
}
    <tr>
        <td>Tổng tiền</td>
        <td>@ViewBag.tongtienoff VNĐ</td>
        <td></td>
    </tr>
</table>
