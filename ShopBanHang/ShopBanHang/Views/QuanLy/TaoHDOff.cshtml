@model IEnumerable<ShopBanHang.Models.KhachHang>

@{
    ViewBag.Title = "TaoHDOff";
    Layout = "~/Views/Shared/_LayoutAdminCT.cshtml";
}
<a href="@Url.Action("TaoKhachHang","QuanLy")">Khách hàng mới</a>
@using (Html.BeginForm("TaoHDOff", "QuanLy"))
{
    @Html.TextBox("tuKhoa")
    <div class="input-group-append">
        <button type="submit" class="btn btn-primary"><i class="fas fa-search fa-sm"></i></button>

    </div>
    <table class="table">
        <tr>
            <th>
               Mã khách hàng
            </th>
            <th>
                Tên khách hàng
            </th>
            <th>
                Địa chỉ khách hàng
            </th>
            <th>
                Số điện thoại
            </th>
            <th>
                Email
            </th>

            
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MaKH)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.tenKH)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DiaChi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SDT)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.email)
                </td>

                
                <td>
                    <a href="@Url.Action("TaoCTHDOff","GioHang", new { @MaKH=item.MaKH})">Chọn</a>

                </td>
            </tr>
        }

    </table>

}