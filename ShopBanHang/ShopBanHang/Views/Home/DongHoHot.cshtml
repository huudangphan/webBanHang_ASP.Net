@model IEnumerable<ShopBanHang.Models.SanPham>

<div class="container">
    <h1>Đồng hồ nổi bật nhất</h1>
    <div class="row" style="margin-top:20px">
        @foreach (var item in Model)
        {

            <div class="col-md-3">
                <div><a href="@Url.Action("xemChiTiet","SanPham",new { @masp=item.maSP})" style="color:white"><img src="~/Assit/img/@Html.DisplayFor(modelItem=>item.anh)" alt="Alternate Text" style="width:230px;height:250px;margin-top:15px;" /></a></div>

                <div style="text-align:center;margin-bottom:10px;">
                    <p style="margin-top:15px;">@Html.DisplayFor(modelItem => item.tenSP)</p>
                    <p>@String.Format("{0:0,0}", Html.DisplayFor(modelItem => item.giaSP)) VNĐ</p>

                </div>

            </div>}
    </div>
</div>
<style>
    .container h1 {
        position: relative;
        margin: 40px 0 30px;
        padding-bottom: 12px;
        color: goldenrod;
    }

        .container h1::before {
            content: "";
            position: absolute;
            display: block;
            width: 100%;
            height: 5px;
            background-color: goldenrod;
            bottom: 0;
        }

    .container .col-md-3 {
        border: 0.5px solid #b7b6b2;
        align-items: center;
    }

        .container .col-md-3 p {
            color: red;
            font-weight: bold;
        }
</style>